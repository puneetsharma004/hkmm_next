/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  images: {
    domains: [
      "rkrcdihogyjnpnffpakq.supabase.co",
    ],
  },
};

export default nextConfig;
